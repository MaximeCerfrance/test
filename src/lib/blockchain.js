// Placeholder for blockchain integration
export function anchorCapsule(data) {
  // TODO: hash data and store on blockchain
  return {
    txId: 'dummy-transaction-id',
    timestamp: Date.now(),
  };
}
